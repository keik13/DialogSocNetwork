# DialogSocNetwork

Для запуска приложения требуется установленные Docker и Docker Compose.

1. Собрать докер образ с приложением `docker build -f docker/Dockerfile -t dialogsocnetwork .`
2. Запустить Docker Compose `docker compose up -d`

Приложение слушает порт 8081.