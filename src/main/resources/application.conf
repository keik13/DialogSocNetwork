config {

  jwt {
    secret = "XWcqd6uCuMlKUXCs4Xcid8rEAQejEByJtjT2p5vVY2s="
    secret = ${?JWT_SECRET}
    issuer = "SocNetwork"
    issuer = ${?ISSUER}
    expireInSeconds = 3600
    expireInSeconds = ${?EXPIRE_IN_SECONDS}
  }

  redisCommonConfig {
    mode = "standalone"
    servers = "localhost:6379"
    password = ""
    maxAttempts = 5
    timeoutMillis = 5000
    masterName = "master01"
  }

  producerConfig {
    topic = "soc_network_saga_event"
    topic = ${?TOPIC}
    bootstrapServers = "localhost:9092"
    bootstrapServers = ${?KAFKA_BOOTSTRAP_SERVERS}
    securityProtocol = "PLAINTEXT"
    securityProtocol = ${?KAFKA_SECURITY_PROTOCOL}
    retries = 2
    retries = ${?KAFKA_PRODUCER_RETRIES}
    maxBlock = 30s
    maxBlock = ${?KAFKA_PRODUCER_MAX_BLOCK}
    deliveryTimeout = 10s
    deliveryTimeout = ${?KAFKA_PRODUCER_DELIVERY_TIMEOUT}
    requestTimeout = 5s
    requestTimeout = ${?KAFKA_PRODUCER_REQUEST_TIMEOUT}
  }
}